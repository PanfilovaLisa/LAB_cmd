-----------PATHES-----------

getPath
    """
    Обрабатывает строку, введённую пользователем.

    Аргументы:
        line: строка, введённая пользователем

        command: файловая команда
        path: путь к файлу/каталогу. Дефолтное значение None.
        option: опция команды. Дефолтное значение None.

    Входные данные: line
    Выходные данные:
        1) Кортеж (команда, путь, опция) - если был задан существующий путь (или не задан)
        2) False + запись в .log - если был введён не существующий путь

    """