import os 
from src.commands import pathes

def mv(path):
    # if len(path)==2:
    #     for way in path:
    #         if pathes.checkRightPath(way):
    #     if all(pathes.checkRightPath(ctlg) for ctlg in path):
    #         os.replace(*path)
    #         return True 
    # return False
    ...